def add(a, b): return a + b
def sub(a, b): return a - b
def mul(a, b): return a * b
def div(a, b): return a / b if b != 0 else "Ошибка"   

import math_operations as m
a=30
b=6
print({m.add(a,b)})
print({m.sub(a,b)})
print({m.mul(a,b)})
print({m.div(a,b)})
