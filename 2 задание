a = input('Введите первое число: ')
b = input('Введите второе число: ')

# Проверяем, что оба ввода состоят только из цифр
if a.isdigit() and b.isdigit(): #метод вовращает True еси все символы - цифры, False - если хоть один - буква
    #Проверяем, что второе число не ноль
    if int(b) != 0:
        print(int(a) / int(b)) #преобра a и b в целые числа, делит а на б и выводит результат
   #обработка ошибок
else:
        print('Ошибка: деление на ноль')
#обработка нечислового ввода
else:
    print('Ошибка: введены нечисловые значения')
