#1 задание
try:
    a, b = int(input("Первое число: ")), int(input("Второе число: ")) #запрос на 2 числа через input и преобразование их в целые числа , знаяения сохраняются в переменные а и b 
    print(a / b) #деление а на б , если б =0 , ошибка
except:
    print("Ошибка")
    
